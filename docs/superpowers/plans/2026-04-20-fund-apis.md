# 资金线 API Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 `develop` 分支上为现有 `*Client` 新增 9 个资金线接口（转账 × 6 + 资金冻结 × 3），通过 TDD 实现并附带示例文件。

**Architecture:** 复用现有模式：类型定义放 `alipay/types/`，接口实现挂在 `*Client` 上，测试使用已有 `newTestClient()` 辅助函数，示例文件放 `example/alipay/`，凭证通过环境变量读取。

**Tech Stack:** Go 1.21+，标准库，无外部依赖。

---

## 文件清单

| 操作 | 路径 | 职责 |
|------|------|------|
| 新增 | `alipay/types/alipay.fund.trans.uni.transfer.go` | 新版单笔转账类型 |
| 新增 | `alipay/alipay.fund.trans.uni.transfer.go` | 新版单笔转账实现 |
| 新增 | `alipay/alipay.fund.trans.uni.transfer_test.go` | 测试 |
| 新增 | `alipay/types/alipay.fund.trans.order.query.go` | 新版转账查询类型 |
| 新增 | `alipay/alipay.fund.trans.order.query.go` | 新版转账查询实现 |
| 新增 | `alipay/alipay.fund.trans.order.query_test.go` | 测试 |
| 新增 | `alipay/types/alipay.fund.trans.refund.go` | 转账撤销类型 |
| 新增 | `alipay/alipay.fund.trans.refund.go` | 转账撤销实现 |
| 新增 | `alipay/alipay.fund.trans.refund_test.go` | 测试 |
| 新增 | `alipay/types/alipay.fund.trans.toalipay.transfer.go` | 旧版转支付宝类型 |
| 新增 | `alipay/alipay.fund.trans.toalipay.transfer.go` | 旧版转支付宝实现 |
| 新增 | `alipay/alipay.fund.trans.toalipay.transfer_test.go` | 测试 |
| 新增 | `alipay/types/alipay.fund.trans.toaccount.transfer.go` | 旧版转账户类型 |
| 新增 | `alipay/alipay.fund.trans.toaccount.transfer.go` | 旧版转账户实现 |
| 新增 | `alipay/alipay.fund.trans.toaccount.transfer_test.go` | 测试 |
| 新增 | `alipay/types/alipay.fund.trans.common.query.go` | 旧版通用查询类型 |
| 新增 | `alipay/alipay.fund.trans.common.query.go` | 旧版通用查询实现 |
| 新增 | `alipay/alipay.fund.trans.common.query_test.go` | 测试 |
| 新增 | `alipay/types/alipay.fund.joint.frozen.go` | 资金冻结类型 |
| 新增 | `alipay/alipay.fund.joint.frozen.go` | 资金冻结实现 |
| 新增 | `alipay/alipay.fund.joint.frozen_test.go` | 测试 |
| 新增 | `alipay/types/alipay.fund.joint.thaw.go` | 资金解冻类型 |
| 新增 | `alipay/alipay.fund.joint.thaw.go` | 资金解冻实现 |
| 新增 | `alipay/alipay.fund.joint.thaw_test.go` | 测试 |
| 新增 | `alipay/types/alipay.fund.joint.deduct.go` | 资金扣款类型 |
| 新增 | `alipay/alipay.fund.joint.deduct.go` | 资金扣款实现 |
| 新增 | `alipay/alipay.fund.joint.deduct_test.go` | 测试 |
| 新增 | `example/alipay/alipay.fund.trans.uni.transfer.go` | 示例 |
| 新增 | `example/alipay/alipay.fund.trans.order.query.go` | 示例 |
| 新增 | `example/alipay/alipay.fund.trans.refund.go` | 示例 |
| 新增 | `example/alipay/alipay.fund.trans.toalipay.transfer.go` | 示例 |
| 新增 | `example/alipay/alipay.fund.trans.toaccount.transfer.go` | 示例 |
| 新增 | `example/alipay/alipay.fund.trans.common.query.go` | 示例 |
| 新增 | `example/alipay/alipay.fund.joint.frozen.go` | 示例 |
| 新增 | `example/alipay/alipay.fund.joint.thaw.go` | 示例 |
| 新增 | `example/alipay/alipay.fund.joint.deduct.go` | 示例 |

---

## Task 1: alipay.fund.trans.uni.transfer（新版单笔转账）

**Files:**
- Create: `alipay/types/alipay.fund.trans.uni.transfer.go`
- Create: `alipay/alipay.fund.trans.uni.transfer.go`
- Create: `alipay/alipay.fund.trans.uni.transfer_test.go`

- [ ] **Step 1: 写失败测试**

创建 `alipay/alipay.fund.trans.uni.transfer_test.go`：

```go
package alipay

import (
	"testing"

	"github.com/godrealms/go-aliyun-sdk/alipay/types"
)

func TestAlipayFundTransUniTransfer(t *testing.T) {
	mockResp := `{"alipay_fund_trans_uni_transfer_response":{"code":"10000","msg":"Success","out_biz_no":"biz001","order_id":"20260420110070000006880000000000","pay_fund_order_id":"20260420110070000006880000000001","status":"SUCCESS","trans_date":"2026-04-20 10:00:00"},"sign":"fakesign"}`

	client, _ := newTestClient(t, mockResp)

	req := &types.FundTransUniTransfer{
		OutBizNo:    "biz001",
		TransAmount: "10.00",
		ProductCode: "TRANS_ACCOUNT_NO_PWD",
		PayeeInfo: &types.FundTransPayee{
			Identity:     "2088000000000000",
			IdentityType: "ALIPAY_USER_ID",
			Name:         "测试用户",
		},
	}
	resp, err := client.AlipayFundTransUniTransfer(req)
	if err != nil {
		t.Fatalf("AlipayFundTransUniTransfer error: %v", err)
	}
	if resp.AlipayFundTransUniTransferResponse.Status != "SUCCESS" {
		t.Errorf("Status = %q, want SUCCESS", resp.AlipayFundTransUniTransferResponse.Status)
	}
	if resp.AlipayFundTransUniTransferResponse.Code != "10000" {
		t.Errorf("Code = %q, want 10000", resp.AlipayFundTransUniTransferResponse.Code)
	}
}
```

- [ ] **Step 2: 运行测试确认失败**

```bash
cd /Volumes/Fanxiang-S790-1TB-Media/Personal/sdk/go-aliyun-sdk
go test ./alipay/ -run TestAlipayFundTransUniTransfer -v
```

预期：`FAIL — types.FundTransUniTransfer undefined`

- [ ] **Step 3: 创建类型文件 `alipay/types/alipay.fund.trans.uni.transfer.go`**

```go
package types

import "encoding/json"

// FundTransPayee 收款方信息
type FundTransPayee struct {
	//【描述】收款方账户，与 identity_type 配合使用
	Identity string `json:"identity"`
	//【描述】账户类型。ALIPAY_USER_ID：支付宝 UID；ALIPAY_LOGON_ID：登录账号（手机/邮箱）
	IdentityType string `json:"identity_type"`
	//【描述】收款方真实姓名，部分场景必填
	Name string `json:"name,omitempty"`
}

// FundTransUniTransfer 单笔转账请求（新版）
type FundTransUniTransfer struct {
	//【描述】商家侧唯一订单号，由商家自定义，不可重复
	//【示例值】201806300001
	OutBizNo string `json:"out_biz_no"`
	//【描述】转账金额，单位：元，精确到小数点后两位
	//【示例值】10.00
	TransAmount string `json:"trans_amount"`
	//【描述】产品码，固定值 TRANS_ACCOUNT_NO_PWD
	//【示例值】TRANS_ACCOUNT_NO_PWD
	ProductCode string `json:"product_code"`
	//【描述】业务场景，默认 DIRECT_TRANSFER
	//【示例值】DIRECT_TRANSFER
	BizScene string `json:"biz_scene,omitempty"`
	//【描述】转账业务的标题，用于在收款方账单中展示
	//【示例值】提现
	OrderTitle string `json:"order_title,omitempty"`
	//【描述】收款方信息
	PayeeInfo *FundTransPayee `json:"payee_info"`
	//【描述】业务备注
	Remark string `json:"remark,omitempty"`
}

func (r *FundTransUniTransfer) ToString() string {
	marshal, _ := json.Marshal(r)
	return string(marshal)
}

// FundTransUniTransferDetail 单笔转账响应详情
type FundTransUniTransferDetail struct {
	PublicResponseParameters
	//【描述】商家侧唯一订单号
	OutBizNo string `json:"out_biz_no"`
	//【描述】支付宝转账单据号
	OrderId string `json:"order_id"`
	//【描述】支付宝支付资金流水号
	PayFundOrderId string `json:"pay_fund_order_id"`
	//【描述】转账单据状态。SUCCESS：成功；FAIL：失败；DEALING：处理中
	Status string `json:"status"`
	//【描述】订单支付时间，格式 yyyy-MM-dd HH:mm:ss
	TransDate string `json:"trans_date"`
}

// AlipayFundTransUniTransferResponse 单笔转账响应
type AlipayFundTransUniTransferResponse struct {
	AlipayFundTransUniTransferResponse FundTransUniTransferDetail `json:"alipay_fund_trans_uni_transfer_response"`
	Sign                               string                     `json:"sign"`
}
```

- [ ] **Step 4: 创建实现文件 `alipay/alipay.fund.trans.uni.transfer.go`**

```go
package alipay

import (
	"context"
	"time"

	"github.com/godrealms/go-aliyun-sdk/alipay/types"
	"github.com/godrealms/go-aliyun-sdk/community"
)

// AlipayFundTransUniTransfer 单笔转账（alipay.fund.trans.uni.transfer）
func (c *Client) AlipayFundTransUniTransfer(request *types.FundTransUniTransfer) (*types.AlipayFundTransUniTransferResponse, error) {
	data := types.PublicRequestParameters{
		AppId:        c.AppId,
		Method:       "alipay.fund.trans.uni.transfer",
		Format:       "JSON",
		Charset:      "UTF-8",
		SignType:     "RSA2",
		Timestamp:    time.Now().Format("2006-01-02 15:04:05"),
		Version:      "1.0",
		AppAuthToken: c.AppAuthToken,
		BizContent:   request.ToString(),
	}

	signature, err := community.NewSignatureHelper(c.PrivateKey)
	if err != nil {
		return nil, err
	}
	data.Sign, err = signature.GenerateSignature(data)
	if err != nil {
		return nil, err
	}

	value := data.ToUrlValue()
	result := &types.AlipayFundTransUniTransferResponse{}
	err = c.Http.PostForm(context.Background(), "", value, nil, result)
	if err != nil {
		return nil, err
	}
	return result, nil
}
```

- [ ] **Step 5: 运行测试确认通过**

```bash
go test ./alipay/ -run TestAlipayFundTransUniTransfer -v
```

预期：`PASS`

- [ ] **Step 6: Commit**

```bash
git add alipay/types/alipay.fund.trans.uni.transfer.go \
        alipay/alipay.fund.trans.uni.transfer.go \
        alipay/alipay.fund.trans.uni.transfer_test.go
git commit -m "feat(fund): 新增 alipay.fund.trans.uni.transfer 接口"
```

---

## Task 2: alipay.fund.trans.order.query（新版转账查询）

**Files:**
- Create: `alipay/types/alipay.fund.trans.order.query.go`
- Create: `alipay/alipay.fund.trans.order.query.go`
- Create: `alipay/alipay.fund.trans.order.query_test.go`

- [ ] **Step 1: 写失败测试**

创建 `alipay/alipay.fund.trans.order.query_test.go`：

```go
package alipay

import (
	"testing"

	"github.com/godrealms/go-aliyun-sdk/alipay/types"
)

func TestAlipayFundTransOrderQuery(t *testing.T) {
	mockResp := `{"alipay_fund_trans_order_query_response":{"code":"10000","msg":"Success","order_id":"20260420110070000006880000000000","status":"SUCCESS","pay_date":"2026-04-20 10:00:00","order_fee":"10.00","fail_reason":""},"sign":"fakesign"}`

	client, _ := newTestClient(t, mockResp)

	req := &types.FundTransOrderQuery{
		OutBizNo: "biz001",
	}
	resp, err := client.AlipayFundTransOrderQuery(req)
	if err != nil {
		t.Fatalf("AlipayFundTransOrderQuery error: %v", err)
	}
	if resp.AlipayFundTransOrderQueryResponse.Status != "SUCCESS" {
		t.Errorf("Status = %q, want SUCCESS", resp.AlipayFundTransOrderQueryResponse.Status)
	}
}
```

- [ ] **Step 2: 运行测试确认失败**

```bash
go test ./alipay/ -run TestAlipayFundTransOrderQuery -v
```

预期：`FAIL — types.FundTransOrderQuery undefined`

- [ ] **Step 3: 创建类型文件 `alipay/types/alipay.fund.trans.order.query.go`**

```go
package types

import "encoding/json"

// FundTransOrderQuery 查询转账订单请求（新版）
type FundTransOrderQuery struct {
	//【描述】商家侧唯一订单号，与 order_id 二选一
	OutBizNo string `json:"out_biz_no,omitempty"`
	//【描述】支付宝转账单据号，与 out_biz_no 二选一
	OrderId string `json:"order_id,omitempty"`
}

func (r *FundTransOrderQuery) ToString() string {
	marshal, _ := json.Marshal(r)
	return string(marshal)
}

// FundTransOrderQueryDetail 查询转账订单响应详情
type FundTransOrderQueryDetail struct {
	PublicResponseParameters
	//【描述】支付宝转账单据号
	OrderId string `json:"order_id"`
	//【描述】转账单据状态。SUCCESS：成功；FAIL：失败；DEALING：处理中
	//【枚举值】SUCCESS | FAIL | DEALING
	Status string `json:"status"`
	//【描述】付款完成时间，格式 yyyy-MM-dd HH:mm:ss
	PayDate string `json:"pay_date"`
	//【描述】预计到账时间（到银行卡场景）
	ArrivalTimeEnd string `json:"arrival_time_end"`
	//【描述】转账金额，单位元
	OrderFee string `json:"order_fee"`
	//【描述】失败原因
	FailReason string `json:"fail_reason"`
}

// AlipayFundTransOrderQueryResponse 查询转账订单响应
type AlipayFundTransOrderQueryResponse struct {
	AlipayFundTransOrderQueryResponse FundTransOrderQueryDetail `json:"alipay_fund_trans_order_query_response"`
	Sign                              string                    `json:"sign"`
}
```

- [ ] **Step 4: 创建实现文件 `alipay/alipay.fund.trans.order.query.go`**

```go
package alipay

import (
	"context"
	"time"

	"github.com/godrealms/go-aliyun-sdk/alipay/types"
	"github.com/godrealms/go-aliyun-sdk/community"
)

// AlipayFundTransOrderQuery 查询转账订单（alipay.fund.trans.order.query）
func (c *Client) AlipayFundTransOrderQuery(request *types.FundTransOrderQuery) (*types.AlipayFundTransOrderQueryResponse, error) {
	data := types.PublicRequestParameters{
		AppId:        c.AppId,
		Method:       "alipay.fund.trans.order.query",
		Format:       "JSON",
		Charset:      "UTF-8",
		SignType:     "RSA2",
		Timestamp:    time.Now().Format("2006-01-02 15:04:05"),
		Version:      "1.0",
		AppAuthToken: c.AppAuthToken,
		BizContent:   request.ToString(),
	}

	signature, err := community.NewSignatureHelper(c.PrivateKey)
	if err != nil {
		return nil, err
	}
	data.Sign, err = signature.GenerateSignature(data)
	if err != nil {
		return nil, err
	}

	value := data.ToUrlValue()
	result := &types.AlipayFundTransOrderQueryResponse{}
	err = c.Http.PostForm(context.Background(), "", value, nil, result)
	if err != nil {
		return nil, err
	}
	return result, nil
}
```

- [ ] **Step 5: 运行测试确认通过**

```bash
go test ./alipay/ -run TestAlipayFundTransOrderQuery -v
```

预期：`PASS`

- [ ] **Step 6: Commit**

```bash
git add alipay/types/alipay.fund.trans.order.query.go \
        alipay/alipay.fund.trans.order.query.go \
        alipay/alipay.fund.trans.order.query_test.go
git commit -m "feat(fund): 新增 alipay.fund.trans.order.query 接口"
```

---

## Task 3: alipay.fund.trans.refund（转账撤销）

**Files:**
- Create: `alipay/types/alipay.fund.trans.refund.go`
- Create: `alipay/alipay.fund.trans.refund.go`
- Create: `alipay/alipay.fund.trans.refund_test.go`

- [ ] **Step 1: 写失败测试**

创建 `alipay/alipay.fund.trans.refund_test.go`：

```go
package alipay

import (
	"testing"

	"github.com/godrealms/go-aliyun-sdk/alipay/types"
)

func TestAlipayFundTransRefund(t *testing.T) {
	mockResp := `{"alipay_fund_trans_refund_response":{"code":"10000","msg":"Success","order_id":"20260420110070000006880000000000","refund_order_id":"20260420110070000006880000000002","refund_amount":"10.00","refund_date":"2026-04-20 10:05:00"},"sign":"fakesign"}`

	client, _ := newTestClient(t, mockResp)

	req := &types.FundTransRefund{
		OutRequestNo: "refund001",
		OrderId:      "20260420110070000006880000000000",
		RefundAmount: "10.00",
	}
	resp, err := client.AlipayFundTransRefund(req)
	if err != nil {
		t.Fatalf("AlipayFundTransRefund error: %v", err)
	}
	if resp.AlipayFundTransRefundResponse.Code != "10000" {
		t.Errorf("Code = %q, want 10000", resp.AlipayFundTransRefundResponse.Code)
	}
	if resp.AlipayFundTransRefundResponse.RefundAmount != "10.00" {
		t.Errorf("RefundAmount = %q, want 10.00", resp.AlipayFundTransRefundResponse.RefundAmount)
	}
}
```

- [ ] **Step 2: 运行测试确认失败**

```bash
go test ./alipay/ -run TestAlipayFundTransRefund -v
```

预期：`FAIL — types.FundTransRefund undefined`

- [ ] **Step 3: 创建类型文件 `alipay/types/alipay.fund.trans.refund.go`**

```go
package types

import "encoding/json"

// FundTransRefund 转账撤销请求
type FundTransRefund struct {
	//【描述】退款请求流水号，由商家自定义，不可重复
	OutRequestNo string `json:"out_request_no"`
	//【描述】支付宝转账单据号，与 out_biz_no 二选一
	OrderId string `json:"order_id,omitempty"`
	//【描述】商家侧唯一订单号，与 order_id 二选一
	OutBizNo string `json:"out_biz_no,omitempty"`
	//【描述】退款金额，单位元，不超过原转账金额
	RefundAmount string `json:"refund_amount"`
	//【描述】退款原因说明
	Remark string `json:"remark,omitempty"`
}

func (r *FundTransRefund) ToString() string {
	marshal, _ := json.Marshal(r)
	return string(marshal)
}

// FundTransRefundDetail 转账撤销响应详情
type FundTransRefundDetail struct {
	PublicResponseParameters
	//【描述】支付宝转账单据号
	OrderId string `json:"order_id"`
	//【描述】退款单据号
	RefundOrderId string `json:"refund_order_id"`
	//【描述】实际退款金额，单位元
	RefundAmount string `json:"refund_amount"`
	//【描述】退款时间，格式 yyyy-MM-dd HH:mm:ss
	RefundDate string `json:"refund_date"`
}

// AlipayFundTransRefundResponse 转账撤销响应
type AlipayFundTransRefundResponse struct {
	AlipayFundTransRefundResponse FundTransRefundDetail `json:"alipay_fund_trans_refund_response"`
	Sign                          string                `json:"sign"`
}
```

- [ ] **Step 4: 创建实现文件 `alipay/alipay.fund.trans.refund.go`**

```go
package alipay

import (
	"context"
	"time"

	"github.com/godrealms/go-aliyun-sdk/alipay/types"
	"github.com/godrealms/go-aliyun-sdk/community"
)

// AlipayFundTransRefund 转账撤销（alipay.fund.trans.refund）
func (c *Client) AlipayFundTransRefund(request *types.FundTransRefund) (*types.AlipayFundTransRefundResponse, error) {
	data := types.PublicRequestParameters{
		AppId:        c.AppId,
		Method:       "alipay.fund.trans.refund",
		Format:       "JSON",
		Charset:      "UTF-8",
		SignType:     "RSA2",
		Timestamp:    time.Now().Format("2006-01-02 15:04:05"),
		Version:      "1.0",
		AppAuthToken: c.AppAuthToken,
		BizContent:   request.ToString(),
	}

	signature, err := community.NewSignatureHelper(c.PrivateKey)
	if err != nil {
		return nil, err
	}
	data.Sign, err = signature.GenerateSignature(data)
	if err != nil {
		return nil, err
	}

	value := data.ToUrlValue()
	result := &types.AlipayFundTransRefundResponse{}
	err = c.Http.PostForm(context.Background(), "", value, nil, result)
	if err != nil {
		return nil, err
	}
	return result, nil
}
```

- [ ] **Step 5: 运行测试确认通过**

```bash
go test ./alipay/ -run TestAlipayFundTransRefund -v
```

预期：`PASS`

- [ ] **Step 6: Commit**

```bash
git add alipay/types/alipay.fund.trans.refund.go \
        alipay/alipay.fund.trans.refund.go \
        alipay/alipay.fund.trans.refund_test.go
git commit -m "feat(fund): 新增 alipay.fund.trans.refund 接口"
```

---

## Task 4: alipay.fund.trans.toalipay.transfer（旧版转支付宝）

**Files:**
- Create: `alipay/types/alipay.fund.trans.toalipay.transfer.go`
- Create: `alipay/alipay.fund.trans.toalipay.transfer.go`
- Create: `alipay/alipay.fund.trans.toalipay.transfer_test.go`

- [ ] **Step 1: 写失败测试**

创建 `alipay/alipay.fund.trans.toalipay.transfer_test.go`：

```go
package alipay

import (
	"testing"

	"github.com/godrealms/go-aliyun-sdk/alipay/types"
)

func TestAlipayFundTransToalipayTransfer(t *testing.T) {
	mockResp := `{"alipay_fund_trans_toalipay_transfer_response":{"code":"10000","msg":"Success","out_biz_no":"biz002","order_id":"20260420110070000006880000000003","pay_date":"2026-04-20 10:00:00"},"sign":"fakesign"}`

	client, _ := newTestClient(t, mockResp)

	req := &types.FundTransToalipayTransfer{
		OutBizNo:     "biz002",
		PayeeType:    "ALIPAY_LOGONID",
		PayeeAccount: "test@example.com",
		Amount:       "5.00",
	}
	resp, err := client.AlipayFundTransToalipayTransfer(req)
	if err != nil {
		t.Fatalf("AlipayFundTransToalipayTransfer error: %v", err)
	}
	if resp.AlipayFundTransToalipayTransferResponse.Code != "10000" {
		t.Errorf("Code = %q, want 10000", resp.AlipayFundTransToalipayTransferResponse.Code)
	}
	if resp.AlipayFundTransToalipayTransferResponse.OrderId != "20260420110070000006880000000003" {
		t.Errorf("OrderId = %q unexpected", resp.AlipayFundTransToalipayTransferResponse.OrderId)
	}
}
```

- [ ] **Step 2: 运行测试确认失败**

```bash
go test ./alipay/ -run TestAlipayFundTransToalipayTransfer -v
```

预期：`FAIL — types.FundTransToalipayTransfer undefined`

- [ ] **Step 3: 创建类型文件 `alipay/types/alipay.fund.trans.toalipay.transfer.go`**

```go
package types

import "encoding/json"

// FundTransToalipayTransfer 转账到支付宝账户请求（旧版）
type FundTransToalipayTransfer struct {
	//【描述】商家流水号，唯一标识转账请求
	OutBizNo string `json:"out_biz_no"`
	//【描述】收款方账户类型。ALIPAY_USERID：支付宝 UID；ALIPAY_LOGONID：登录账号
	//【枚举值】ALIPAY_USERID | ALIPAY_LOGONID
	PayeeType string `json:"payee_type"`
	//【描述】收款方账户
	PayeeAccount string `json:"payee_account"`
	//【描述】转账金额，单位元，精确到小数点后两位
	Amount string `json:"amount"`
	//【描述】收款方真实姓名，与证件一致（部分场景必填）
	PayeeRealName string `json:"payee_real_name,omitempty"`
	//【描述】转账备注
	Remark string `json:"remark,omitempty"`
}

func (r *FundTransToalipayTransfer) ToString() string {
	marshal, _ := json.Marshal(r)
	return string(marshal)
}

// FundTransToalipayTransferDetail 转账到支付宝账户响应详情
type FundTransToalipayTransferDetail struct {
	PublicResponseParameters
	//【描述】商家流水号
	OutBizNo string `json:"out_biz_no"`
	//【描述】支付宝转账单据号
	OrderId string `json:"order_id"`
	//【描述】支付时间，格式 yyyy-MM-dd HH:mm:ss
	PayDate string `json:"pay_date"`
}

// AlipayFundTransToalipayTransferResponse 转账到支付宝账户响应
type AlipayFundTransToalipayTransferResponse struct {
	AlipayFundTransToalipayTransferResponse FundTransToalipayTransferDetail `json:"alipay_fund_trans_toalipay_transfer_response"`
	Sign                                    string                          `json:"sign"`
}
```

- [ ] **Step 4: 创建实现文件 `alipay/alipay.fund.trans.toalipay.transfer.go`**

```go
package alipay

import (
	"context"
	"time"

	"github.com/godrealms/go-aliyun-sdk/alipay/types"
	"github.com/godrealms/go-aliyun-sdk/community"
)

// AlipayFundTransToalipayTransfer 转账到支付宝账户（alipay.fund.trans.toalipay.transfer）旧版
func (c *Client) AlipayFundTransToalipayTransfer(request *types.FundTransToalipayTransfer) (*types.AlipayFundTransToalipayTransferResponse, error) {
	data := types.PublicRequestParameters{
		AppId:        c.AppId,
		Method:       "alipay.fund.trans.toalipay.transfer",
		Format:       "JSON",
		Charset:      "UTF-8",
		SignType:     "RSA2",
		Timestamp:    time.Now().Format("2006-01-02 15:04:05"),
		Version:      "1.0",
		AppAuthToken: c.AppAuthToken,
		BizContent:   request.ToString(),
	}

	signature, err := community.NewSignatureHelper(c.PrivateKey)
	if err != nil {
		return nil, err
	}
	data.Sign, err = signature.GenerateSignature(data)
	if err != nil {
		return nil, err
	}

	value := data.ToUrlValue()
	result := &types.AlipayFundTransToalipayTransferResponse{}
	err = c.Http.PostForm(context.Background(), "", value, nil, result)
	if err != nil {
		return nil, err
	}
	return result, nil
}
```

- [ ] **Step 5: 运行测试确认通过**

```bash
go test ./alipay/ -run TestAlipayFundTransToalipayTransfer -v
```

预期：`PASS`

- [ ] **Step 6: Commit**

```bash
git add alipay/types/alipay.fund.trans.toalipay.transfer.go \
        alipay/alipay.fund.trans.toalipay.transfer.go \
        alipay/alipay.fund.trans.toalipay.transfer_test.go
git commit -m "feat(fund): 新增 alipay.fund.trans.toalipay.transfer 接口"
```

---

## Task 5: alipay.fund.trans.toaccount.transfer（旧版转账户）

**Files:**
- Create: `alipay/types/alipay.fund.trans.toaccount.transfer.go`
- Create: `alipay/alipay.fund.trans.toaccount.transfer.go`
- Create: `alipay/alipay.fund.trans.toaccount.transfer_test.go`

- [ ] **Step 1: 写失败测试**

创建 `alipay/alipay.fund.trans.toaccount.transfer_test.go`：

```go
package alipay

import (
	"testing"

	"github.com/godrealms/go-aliyun-sdk/alipay/types"
)

func TestAlipayFundTransToaccountTransfer(t *testing.T) {
	mockResp := `{"alipay_fund_trans_toaccount_transfer_response":{"code":"10000","msg":"Success","out_biz_no":"biz003","order_id":"20260420110070000006880000000004","pay_date":"2026-04-20 10:00:00"},"sign":"fakesign"}`

	client, _ := newTestClient(t, mockResp)

	req := &types.FundTransToaccountTransfer{
		OutBizNo:     "biz003",
		PayeeType:    "ALIPAY_LOGONID",
		PayeeAccount: "test@example.com",
		Amount:       "5.00",
	}
	resp, err := client.AlipayFundTransToaccountTransfer(req)
	if err != nil {
		t.Fatalf("AlipayFundTransToaccountTransfer error: %v", err)
	}
	if resp.AlipayFundTransToaccountTransferResponse.Code != "10000" {
		t.Errorf("Code = %q, want 10000", resp.AlipayFundTransToaccountTransferResponse.Code)
	}
}
```

- [ ] **Step 2: 运行测试确认失败**

```bash
go test ./alipay/ -run TestAlipayFundTransToaccountTransfer -v
```

预期：`FAIL — types.FundTransToaccountTransfer undefined`

- [ ] **Step 3: 创建类型文件 `alipay/types/alipay.fund.trans.toaccount.transfer.go`**

```go
package types

import "encoding/json"

// FundTransToaccountTransfer 转账到账户请求（旧版）
type FundTransToaccountTransfer struct {
	//【描述】商家流水号，唯一标识转账请求
	OutBizNo string `json:"out_biz_no"`
	//【描述】收款方账户类型。ALIPAY_USERID | ALIPAY_LOGONID | ALIPAY_OPENID
	PayeeType string `json:"payee_type"`
	//【描述】收款方账户
	PayeeAccount string `json:"payee_account"`
	//【描述】转账金额，单位元
	Amount string `json:"amount"`
	//【描述】付款方显示姓名
	PayerShowName string `json:"payer_show_name,omitempty"`
	//【描述】收款方真实姓名
	PayeeRealName string `json:"payee_real_name,omitempty"`
	//【描述】转账备注
	Remark string `json:"remark,omitempty"`
}

func (r *FundTransToaccountTransfer) ToString() string {
	marshal, _ := json.Marshal(r)
	return string(marshal)
}

// FundTransToaccountTransferDetail 转账到账户响应详情
type FundTransToaccountTransferDetail struct {
	PublicResponseParameters
	//【描述】商家流水号
	OutBizNo string `json:"out_biz_no"`
	//【描述】支付宝转账单据号
	OrderId string `json:"order_id"`
	//【描述】支付时间，格式 yyyy-MM-dd HH:mm:ss
	PayDate string `json:"pay_date"`
}

// AlipayFundTransToaccountTransferResponse 转账到账户响应
type AlipayFundTransToaccountTransferResponse struct {
	AlipayFundTransToaccountTransferResponse FundTransToaccountTransferDetail `json:"alipay_fund_trans_toaccount_transfer_response"`
	Sign                                     string                           `json:"sign"`
}
```

- [ ] **Step 4: 创建实现文件 `alipay/alipay.fund.trans.toaccount.transfer.go`**

```go
package alipay

import (
	"context"
	"time"

	"github.com/godrealms/go-aliyun-sdk/alipay/types"
	"github.com/godrealms/go-aliyun-sdk/community"
)

// AlipayFundTransToaccountTransfer 转账到账户（alipay.fund.trans.toaccount.transfer）旧版
func (c *Client) AlipayFundTransToaccountTransfer(request *types.FundTransToaccountTransfer) (*types.AlipayFundTransToaccountTransferResponse, error) {
	data := types.PublicRequestParameters{
		AppId:        c.AppId,
		Method:       "alipay.fund.trans.toaccount.transfer",
		Format:       "JSON",
		Charset:      "UTF-8",
		SignType:     "RSA2",
		Timestamp:    time.Now().Format("2006-01-02 15:04:05"),
		Version:      "1.0",
		AppAuthToken: c.AppAuthToken,
		BizContent:   request.ToString(),
	}

	signature, err := community.NewSignatureHelper(c.PrivateKey)
	if err != nil {
		return nil, err
	}
	data.Sign, err = signature.GenerateSignature(data)
	if err != nil {
		return nil, err
	}

	value := data.ToUrlValue()
	result := &types.AlipayFundTransToaccountTransferResponse{}
	err = c.Http.PostForm(context.Background(), "", value, nil, result)
	if err != nil {
		return nil, err
	}
	return result, nil
}
```

- [ ] **Step 5: 运行测试确认通过**

```bash
go test ./alipay/ -run TestAlipayFundTransToaccountTransfer -v
```

预期：`PASS`

- [ ] **Step 6: Commit**

```bash
git add alipay/types/alipay.fund.trans.toaccount.transfer.go \
        alipay/alipay.fund.trans.toaccount.transfer.go \
        alipay/alipay.fund.trans.toaccount.transfer_test.go
git commit -m "feat(fund): 新增 alipay.fund.trans.toaccount.transfer 接口"
```

---

## Task 6: alipay.fund.trans.common.query（旧版通用查询）

**Files:**
- Create: `alipay/types/alipay.fund.trans.common.query.go`
- Create: `alipay/alipay.fund.trans.common.query.go`
- Create: `alipay/alipay.fund.trans.common.query_test.go`

- [ ] **Step 1: 写失败测试**

创建 `alipay/alipay.fund.trans.common.query_test.go`：

```go
package alipay

import (
	"testing"

	"github.com/godrealms/go-aliyun-sdk/alipay/types"
)

func TestAlipayFundTransCommonQuery(t *testing.T) {
	mockResp := `{"alipay_fund_trans_common_query_response":{"code":"10000","msg":"Success","order_id":"20260420110070000006880000000000","pay_fund_order_id":"20260420110070000006880000000001","status":"SUCCESS","pay_date":"2026-04-20 10:00:00","order_fee":"10.00"},"sign":"fakesign"}`

	client, _ := newTestClient(t, mockResp)

	req := &types.FundTransCommonQuery{
		OutBizNo: "biz001",
	}
	resp, err := client.AlipayFundTransCommonQuery(req)
	if err != nil {
		t.Fatalf("AlipayFundTransCommonQuery error: %v", err)
	}
	if resp.AlipayFundTransCommonQueryResponse.Status != "SUCCESS" {
		t.Errorf("Status = %q, want SUCCESS", resp.AlipayFundTransCommonQueryResponse.Status)
	}
}
```

- [ ] **Step 2: 运行测试确认失败**

```bash
go test ./alipay/ -run TestAlipayFundTransCommonQuery -v
```

预期：`FAIL — types.FundTransCommonQuery undefined`

- [ ] **Step 3: 创建类型文件 `alipay/types/alipay.fund.trans.common.query.go`**

```go
package types

import "encoding/json"

// FundTransCommonQuery 通用转账查询请求（旧版）
type FundTransCommonQuery struct {
	//【描述】产品码，与转账时一致
	ProductCode string `json:"product_code,omitempty"`
	//【描述】业务场景
	BizScene string `json:"biz_scene,omitempty"`
	//【描述】商家侧唯一订单号，与 order_id 二选一
	OutBizNo string `json:"out_biz_no,omitempty"`
	//【描述】支付宝转账单据号，与 out_biz_no 二选一
	OrderId string `json:"order_id,omitempty"`
	//【描述】支付宝支付资金流水号
	PayFundOrderId string `json:"pay_fund_order_id,omitempty"`
}

func (r *FundTransCommonQuery) ToString() string {
	marshal, _ := json.Marshal(r)
	return string(marshal)
}

// FundTransCommonQueryDetail 通用转账查询响应详情
type FundTransCommonQueryDetail struct {
	PublicResponseParameters
	//【描述】支付宝转账单据号
	OrderId string `json:"order_id"`
	//【描述】支付宝支付资金流水号
	PayFundOrderId string `json:"pay_fund_order_id"`
	//【描述】转账单据状态。SUCCESS | FAIL | DEALING
	Status string `json:"status"`
	//【描述】付款完成时间，格式 yyyy-MM-dd HH:mm:ss
	PayDate string `json:"pay_date"`
	//【描述】转账金额，单位元
	OrderFee string `json:"order_fee"`
	//【描述】失败原因
	FailReason string `json:"fail_reason"`
}

// AlipayFundTransCommonQueryResponse 通用转账查询响应
type AlipayFundTransCommonQueryResponse struct {
	AlipayFundTransCommonQueryResponse FundTransCommonQueryDetail `json:"alipay_fund_trans_common_query_response"`
	Sign                               string                     `json:"sign"`
}
```

- [ ] **Step 4: 创建实现文件 `alipay/alipay.fund.trans.common.query.go`**

```go
package alipay

import (
	"context"
	"time"

	"github.com/godrealms/go-aliyun-sdk/alipay/types"
	"github.com/godrealms/go-aliyun-sdk/community"
)

// AlipayFundTransCommonQuery 通用转账查询（alipay.fund.trans.common.query）旧版
func (c *Client) AlipayFundTransCommonQuery(request *types.FundTransCommonQuery) (*types.AlipayFundTransCommonQueryResponse, error) {
	data := types.PublicRequestParameters{
		AppId:        c.AppId,
		Method:       "alipay.fund.trans.common.query",
		Format:       "JSON",
		Charset:      "UTF-8",
		SignType:     "RSA2",
		Timestamp:    time.Now().Format("2006-01-02 15:04:05"),
		Version:      "1.0",
		AppAuthToken: c.AppAuthToken,
		BizContent:   request.ToString(),
	}

	signature, err := community.NewSignatureHelper(c.PrivateKey)
	if err != nil {
		return nil, err
	}
	data.Sign, err = signature.GenerateSignature(data)
	if err != nil {
		return nil, err
	}

	value := data.ToUrlValue()
	result := &types.AlipayFundTransCommonQueryResponse{}
	err = c.Http.PostForm(context.Background(), "", value, nil, result)
	if err != nil {
		return nil, err
	}
	return result, nil
}
```

- [ ] **Step 5: 运行测试确认通过**

```bash
go test ./alipay/ -run TestAlipayFundTransCommonQuery -v
```

预期：`PASS`

- [ ] **Step 6: Commit**

```bash
git add alipay/types/alipay.fund.trans.common.query.go \
        alipay/alipay.fund.trans.common.query.go \
        alipay/alipay.fund.trans.common.query_test.go
git commit -m "feat(fund): 新增 alipay.fund.trans.common.query 接口"
```

---

## Task 7: alipay.fund.joint.frozen（资金冻结）

**Files:**
- Create: `alipay/types/alipay.fund.joint.frozen.go`
- Create: `alipay/alipay.fund.joint.frozen.go`
- Create: `alipay/alipay.fund.joint.frozen_test.go`

- [ ] **Step 1: 写失败测试**

创建 `alipay/alipay.fund.joint.frozen_test.go`：

```go
package alipay

import (
	"testing"

	"github.com/godrealms/go-aliyun-sdk/alipay/types"
)

func TestAlipayFundJointFrozen(t *testing.T) {
	mockResp := `{"alipay_fund_joint_frozen_response":{"code":"10000","msg":"Success","freeze_id":"2026042022001480551404403022","freeze_date":"2026-04-20 10:00:00","amount":"50.00"},"sign":"fakesign"}`

	client, _ := newTestClient(t, mockResp)

	req := &types.FundJointFrozen{
		OutRequestNo: "freeze001",
		PayerUserId:  "2088000000000000",
		Amount:       "50.00",
		Remark:       "押金冻结",
	}
	resp, err := client.AlipayFundJointFrozen(req)
	if err != nil {
		t.Fatalf("AlipayFundJointFrozen error: %v", err)
	}
	if resp.AlipayFundJointFrozenResponse.Code != "10000" {
		t.Errorf("Code = %q, want 10000", resp.AlipayFundJointFrozenResponse.Code)
	}
	if resp.AlipayFundJointFrozenResponse.FreezeId != "2026042022001480551404403022" {
		t.Errorf("FreezeId = %q unexpected", resp.AlipayFundJointFrozenResponse.FreezeId)
	}
}
```

- [ ] **Step 2: 运行测试确认失败**

```bash
go test ./alipay/ -run TestAlipayFundJointFrozen -v
```

预期：`FAIL — types.FundJointFrozen undefined`

- [ ] **Step 3: 创建类型文件 `alipay/types/alipay.fund.joint.frozen.go`**

```go
package types

import "encoding/json"

// FundJointFrozen 资金冻结请求
type FundJointFrozen struct {
	//【描述】商户冻结流水号，唯一标识本次冻结请求
	OutRequestNo string `json:"out_request_no"`
	//【描述】付款方支付宝 UID
	PayerUserId string `json:"payer_user_id"`
	//【描述】冻结金额，单位元，精确到小数点后两位
	Amount string `json:"amount"`
	//【描述】冻结说明，用于收款方账单展示
	Remark string `json:"remark,omitempty"`
	//【描述】扩展参数，JSON 字符串格式
	ExtraParam string `json:"extra_param,omitempty"`
}

func (r *FundJointFrozen) ToString() string {
	marshal, _ := json.Marshal(r)
	return string(marshal)
}

// FundJointFrozenDetail 资金冻结响应详情
type FundJointFrozenDetail struct {
	PublicResponseParameters
	//【描述】支付宝冻结单号
	FreezeId string `json:"freeze_id"`
	//【描述】冻结时间，格式 yyyy-MM-dd HH:mm:ss
	FreezeDate string `json:"freeze_date"`
	//【描述】实际冻结金额，单位元
	Amount string `json:"amount"`
}

// AlipayFundJointFrozenResponse 资金冻结响应
type AlipayFundJointFrozenResponse struct {
	AlipayFundJointFrozenResponse FundJointFrozenDetail `json:"alipay_fund_joint_frozen_response"`
	Sign                          string                `json:"sign"`
}
```

- [ ] **Step 4: 创建实现文件 `alipay/alipay.fund.joint.frozen.go`**

```go
package alipay

import (
	"context"
	"time"

	"github.com/godrealms/go-aliyun-sdk/alipay/types"
	"github.com/godrealms/go-aliyun-sdk/community"
)

// AlipayFundJointFrozen 资金冻结（alipay.fund.joint.frozen）
func (c *Client) AlipayFundJointFrozen(request *types.FundJointFrozen) (*types.AlipayFundJointFrozenResponse, error) {
	data := types.PublicRequestParameters{
		AppId:        c.AppId,
		Method:       "alipay.fund.joint.frozen",
		Format:       "JSON",
		Charset:      "UTF-8",
		SignType:     "RSA2",
		Timestamp:    time.Now().Format("2006-01-02 15:04:05"),
		Version:      "1.0",
		AppAuthToken: c.AppAuthToken,
		BizContent:   request.ToString(),
	}

	signature, err := community.NewSignatureHelper(c.PrivateKey)
	if err != nil {
		return nil, err
	}
	data.Sign, err = signature.GenerateSignature(data)
	if err != nil {
		return nil, err
	}

	value := data.ToUrlValue()
	result := &types.AlipayFundJointFrozenResponse{}
	err = c.Http.PostForm(context.Background(), "", value, nil, result)
	if err != nil {
		return nil, err
	}
	return result, nil
}
```

- [ ] **Step 5: 运行测试确认通过**

```bash
go test ./alipay/ -run TestAlipayFundJointFrozen -v
```

预期：`PASS`

- [ ] **Step 6: Commit**

```bash
git add alipay/types/alipay.fund.joint.frozen.go \
        alipay/alipay.fund.joint.frozen.go \
        alipay/alipay.fund.joint.frozen_test.go
git commit -m "feat(fund): 新增 alipay.fund.joint.frozen 接口"
```

---

## Task 8: alipay.fund.joint.thaw（资金解冻）

**Files:**
- Create: `alipay/types/alipay.fund.joint.thaw.go`
- Create: `alipay/alipay.fund.joint.thaw.go`
- Create: `alipay/alipay.fund.joint.thaw_test.go`

- [ ] **Step 1: 写失败测试**

创建 `alipay/alipay.fund.joint.thaw_test.go`：

```go
package alipay

import (
	"testing"

	"github.com/godrealms/go-aliyun-sdk/alipay/types"
)

func TestAlipayFundJointThaw(t *testing.T) {
	mockResp := `{"alipay_fund_joint_thaw_response":{"code":"10000","msg":"Success","out_request_no":"thaw001","amount":"50.00","thaw_date":"2026-04-20 11:00:00"},"sign":"fakesign"}`

	client, _ := newTestClient(t, mockResp)

	req := &types.FundJointThaw{
		OutRequestNo: "thaw001",
		FreezeId:     "2026042022001480551404403022",
		Amount:       "50.00",
	}
	resp, err := client.AlipayFundJointThaw(req)
	if err != nil {
		t.Fatalf("AlipayFundJointThaw error: %v", err)
	}
	if resp.AlipayFundJointThawResponse.Code != "10000" {
		t.Errorf("Code = %q, want 10000", resp.AlipayFundJointThawResponse.Code)
	}
}
```

- [ ] **Step 2: 运行测试确认失败**

```bash
go test ./alipay/ -run TestAlipayFundJointThaw -v
```

预期：`FAIL — types.FundJointThaw undefined`

- [ ] **Step 3: 创建类型文件 `alipay/types/alipay.fund.joint.thaw.go`**

```go
package types

import "encoding/json"

// FundJointThaw 资金解冻请求
type FundJointThaw struct {
	//【描述】商户解冻流水号，唯一标识本次解冻请求
	OutRequestNo string `json:"out_request_no"`
	//【描述】支付宝冻结单号，由冻结接口返回
	FreezeId string `json:"freeze_id"`
	//【描述】解冻金额，单位元，不超过冻结金额
	Amount string `json:"amount"`
	//【描述】解冻说明
	Remark string `json:"remark,omitempty"`
}

func (r *FundJointThaw) ToString() string {
	marshal, _ := json.Marshal(r)
	return string(marshal)
}

// FundJointThawDetail 资金解冻响应详情
type FundJointThawDetail struct {
	PublicResponseParameters
	//【描述】商户解冻流水号
	OutRequestNo string `json:"out_request_no"`
	//【描述】实际解冻金额，单位元
	Amount string `json:"amount"`
	//【描述】解冻时间，格式 yyyy-MM-dd HH:mm:ss
	ThawDate string `json:"thaw_date"`
}

// AlipayFundJointThawResponse 资金解冻响应
type AlipayFundJointThawResponse struct {
	AlipayFundJointThawResponse FundJointThawDetail `json:"alipay_fund_joint_thaw_response"`
	Sign                        string              `json:"sign"`
}
```

- [ ] **Step 4: 创建实现文件 `alipay/alipay.fund.joint.thaw.go`**

```go
package alipay

import (
	"context"
	"time"

	"github.com/godrealms/go-aliyun-sdk/alipay/types"
	"github.com/godrealms/go-aliyun-sdk/community"
)

// AlipayFundJointThaw 资金解冻（alipay.fund.joint.thaw）
func (c *Client) AlipayFundJointThaw(request *types.FundJointThaw) (*types.AlipayFundJointThawResponse, error) {
	data := types.PublicRequestParameters{
		AppId:        c.AppId,
		Method:       "alipay.fund.joint.thaw",
		Format:       "JSON",
		Charset:      "UTF-8",
		SignType:     "RSA2",
		Timestamp:    time.Now().Format("2006-01-02 15:04:05"),
		Version:      "1.0",
		AppAuthToken: c.AppAuthToken,
		BizContent:   request.ToString(),
	}

	signature, err := community.NewSignatureHelper(c.PrivateKey)
	if err != nil {
		return nil, err
	}
	data.Sign, err = signature.GenerateSignature(data)
	if err != nil {
		return nil, err
	}

	value := data.ToUrlValue()
	result := &types.AlipayFundJointThawResponse{}
	err = c.Http.PostForm(context.Background(), "", value, nil, result)
	if err != nil {
		return nil, err
	}
	return result, nil
}
```

- [ ] **Step 5: 运行测试确认通过**

```bash
go test ./alipay/ -run TestAlipayFundJointThaw -v
```

预期：`PASS`

- [ ] **Step 6: Commit**

```bash
git add alipay/types/alipay.fund.joint.thaw.go \
        alipay/alipay.fund.joint.thaw.go \
        alipay/alipay.fund.joint.thaw_test.go
git commit -m "feat(fund): 新增 alipay.fund.joint.thaw 接口"
```

---

## Task 9: alipay.fund.joint.deduct（资金扣款）

**Files:**
- Create: `alipay/types/alipay.fund.joint.deduct.go`
- Create: `alipay/alipay.fund.joint.deduct.go`
- Create: `alipay/alipay.fund.joint.deduct_test.go`

- [ ] **Step 1: 写失败测试**

创建 `alipay/alipay.fund.joint.deduct_test.go`：

```go
package alipay

import (
	"testing"

	"github.com/godrealms/go-aliyun-sdk/alipay/types"
)

func TestAlipayFundJointDeduct(t *testing.T) {
	mockResp := `{"alipay_fund_joint_deduct_response":{"code":"10000","msg":"Success","out_request_no":"deduct001","order_id":"20260420110070000006880000000005","amount":"30.00","deduct_date":"2026-04-20 12:00:00"},"sign":"fakesign"}`

	client, _ := newTestClient(t, mockResp)

	req := &types.FundJointDeduct{
		OutRequestNo: "deduct001",
		FreezeId:     "2026042022001480551404403022",
		Amount:       "30.00",
		Remark:       "扣除押金",
	}
	resp, err := client.AlipayFundJointDeduct(req)
	if err != nil {
		t.Fatalf("AlipayFundJointDeduct error: %v", err)
	}
	if resp.AlipayFundJointDeductResponse.Code != "10000" {
		t.Errorf("Code = %q, want 10000", resp.AlipayFundJointDeductResponse.Code)
	}
	if resp.AlipayFundJointDeductResponse.Amount != "30.00" {
		t.Errorf("Amount = %q, want 30.00", resp.AlipayFundJointDeductResponse.Amount)
	}
}
```

- [ ] **Step 2: 运行测试确认失败**

```bash
go test ./alipay/ -run TestAlipayFundJointDeduct -v
```

预期：`FAIL — types.FundJointDeduct undefined`

- [ ] **Step 3: 创建类型文件 `alipay/types/alipay.fund.joint.deduct.go`**

```go
package types

import "encoding/json"

// FundJointDeduct 从冻结金额扣款请求
type FundJointDeduct struct {
	//【描述】商户扣款流水号，唯一标识本次扣款请求
	OutRequestNo string `json:"out_request_no"`
	//【描述】支付宝冻结单号，由冻结接口返回
	FreezeId string `json:"freeze_id"`
	//【描述】扣款金额，单位元，不超过冻结金额
	Amount string `json:"amount"`
	//【描述】转入方支付宝账号（UID 或登录账号），不填则转入商户自身账号
	TransIn string `json:"trans_in,omitempty"`
	//【描述】扣款说明
	Remark string `json:"remark,omitempty"`
}

func (r *FundJointDeduct) ToString() string {
	marshal, _ := json.Marshal(r)
	return string(marshal)
}

// FundJointDeductDetail 从冻结金额扣款响应详情
type FundJointDeductDetail struct {
	PublicResponseParameters
	//【描述】商户扣款流水号
	OutRequestNo string `json:"out_request_no"`
	//【描述】支付宝扣款单号
	OrderId string `json:"order_id"`
	//【描述】实际扣款金额，单位元
	Amount string `json:"amount"`
	//【描述】扣款时间，格式 yyyy-MM-dd HH:mm:ss
	DeductDate string `json:"deduct_date"`
}

// AlipayFundJointDeductResponse 从冻结金额扣款响应
type AlipayFundJointDeductResponse struct {
	AlipayFundJointDeductResponse FundJointDeductDetail `json:"alipay_fund_joint_deduct_response"`
	Sign                          string                `json:"sign"`
}
```

- [ ] **Step 4: 创建实现文件 `alipay/alipay.fund.joint.deduct.go`**

```go
package alipay

import (
	"context"
	"time"

	"github.com/godrealms/go-aliyun-sdk/alipay/types"
	"github.com/godrealms/go-aliyun-sdk/community"
)

// AlipayFundJointDeduct 从冻结金额扣款（alipay.fund.joint.deduct）
func (c *Client) AlipayFundJointDeduct(request *types.FundJointDeduct) (*types.AlipayFundJointDeductResponse, error) {
	data := types.PublicRequestParameters{
		AppId:        c.AppId,
		Method:       "alipay.fund.joint.deduct",
		Format:       "JSON",
		Charset:      "UTF-8",
		SignType:     "RSA2",
		Timestamp:    time.Now().Format("2006-01-02 15:04:05"),
		Version:      "1.0",
		AppAuthToken: c.AppAuthToken,
		BizContent:   request.ToString(),
	}

	signature, err := community.NewSignatureHelper(c.PrivateKey)
	if err != nil {
		return nil, err
	}
	data.Sign, err = signature.GenerateSignature(data)
	if err != nil {
		return nil, err
	}

	value := data.ToUrlValue()
	result := &types.AlipayFundJointDeductResponse{}
	err = c.Http.PostForm(context.Background(), "", value, nil, result)
	if err != nil {
		return nil, err
	}
	return result, nil
}
```

- [ ] **Step 5: 运行测试确认通过**

```bash
go test ./alipay/ -run TestAlipayFundJointDeduct -v
```

预期：`PASS`

- [ ] **Step 6: Commit**

```bash
git add alipay/types/alipay.fund.joint.deduct.go \
        alipay/alipay.fund.joint.deduct.go \
        alipay/alipay.fund.joint.deduct_test.go
git commit -m "feat(fund): 新增 alipay.fund.joint.deduct 接口"
```

---

## Task 10: 全量测试验证

- [ ] **Step 1: 运行所有测试**

```bash
go test ./alipay/ -v
```

预期：所有测试 `PASS`，含 Task 1-9 新增的 9 个测试函数

- [ ] **Step 2: 构建和静态检查**

```bash
go build github.com/godrealms/go-aliyun-sdk/alipay \
         github.com/godrealms/go-aliyun-sdk/alipay/types \
         github.com/godrealms/go-aliyun-sdk/community \
         github.com/godrealms/go-aliyun-sdk/utils
go vet ./alipay/ ./alipay/types/
```

预期：无错误输出

---

## Task 11: 示例文件

**Files:** 9 个示例文件，均在 `example/alipay/`

- [ ] **Step 1: 创建 `example/alipay/alipay.fund.trans.uni.transfer.go`**

```go
package main

import (
	"log"
	"os"

	"github.com/godrealms/go-aliyun-sdk/alipay"
	"github.com/godrealms/go-aliyun-sdk/alipay/types"
)

func main() {
	// Required env vars: ALIPAY_APP_ID, ALIPAY_PRIVATE_KEY, ALIPAY_PUBLIC_KEY_FROM_ALIPAY
	client := alipay.NewClient()
	client.AppId = os.Getenv("ALIPAY_APP_ID")
	client.PrivateKey = os.Getenv("ALIPAY_PRIVATE_KEY")
	client.AlipayPublicKey = os.Getenv("ALIPAY_PUBLIC_KEY_FROM_ALIPAY")
	client.Sandbox = true
	client.Http.SetBaseURL("https://openapi-sandbox.dl.alipaydev.com/gateway.do")

	resp, err := client.AlipayFundTransUniTransfer(&types.FundTransUniTransfer{
		OutBizNo:    "biz_20260420_001",
		TransAmount: "10.00",
		ProductCode: "TRANS_ACCOUNT_NO_PWD",
		OrderTitle:  "提现",
		PayeeInfo: &types.FundTransPayee{
			Identity:     "2088000000000000",
			IdentityType: "ALIPAY_USER_ID",
			Name:         "收款方姓名",
		},
	})
	if err != nil {
		log.Fatal(err)
	}
	log.Printf("Status: %s, OrderId: %s", resp.AlipayFundTransUniTransferResponse.Status, resp.AlipayFundTransUniTransferResponse.OrderId)
}
```

- [ ] **Step 2: 创建 `example/alipay/alipay.fund.trans.order.query.go`**

```go
package main

import (
	"log"
	"os"

	"github.com/godrealms/go-aliyun-sdk/alipay"
	"github.com/godrealms/go-aliyun-sdk/alipay/types"
)

func main() {
	// Required env vars: ALIPAY_APP_ID, ALIPAY_PRIVATE_KEY, ALIPAY_PUBLIC_KEY_FROM_ALIPAY
	client := alipay.NewClient()
	client.AppId = os.Getenv("ALIPAY_APP_ID")
	client.PrivateKey = os.Getenv("ALIPAY_PRIVATE_KEY")
	client.AlipayPublicKey = os.Getenv("ALIPAY_PUBLIC_KEY_FROM_ALIPAY")
	client.Sandbox = true
	client.Http.SetBaseURL("https://openapi-sandbox.dl.alipaydev.com/gateway.do")

	resp, err := client.AlipayFundTransOrderQuery(&types.FundTransOrderQuery{
		OutBizNo: "biz_20260420_001",
	})
	if err != nil {
		log.Fatal(err)
	}
	log.Printf("Status: %s, OrderFee: %s", resp.AlipayFundTransOrderQueryResponse.Status, resp.AlipayFundTransOrderQueryResponse.OrderFee)
}
```

- [ ] **Step 3: 创建 `example/alipay/alipay.fund.trans.refund.go`**

```go
package main

import (
	"log"
	"os"

	"github.com/godrealms/go-aliyun-sdk/alipay"
	"github.com/godrealms/go-aliyun-sdk/alipay/types"
)

func main() {
	// Required env vars: ALIPAY_APP_ID, ALIPAY_PRIVATE_KEY, ALIPAY_PUBLIC_KEY_FROM_ALIPAY
	client := alipay.NewClient()
	client.AppId = os.Getenv("ALIPAY_APP_ID")
	client.PrivateKey = os.Getenv("ALIPAY_PRIVATE_KEY")
	client.AlipayPublicKey = os.Getenv("ALIPAY_PUBLIC_KEY_FROM_ALIPAY")
	client.Sandbox = true
	client.Http.SetBaseURL("https://openapi-sandbox.dl.alipaydev.com/gateway.do")

	resp, err := client.AlipayFundTransRefund(&types.FundTransRefund{
		OutRequestNo: "refund_20260420_001",
		OutBizNo:     "biz_20260420_001",
		RefundAmount: "10.00",
		Remark:       "撤销转账",
	})
	if err != nil {
		log.Fatal(err)
	}
	log.Printf("Code: %s, RefundOrderId: %s", resp.AlipayFundTransRefundResponse.Code, resp.AlipayFundTransRefundResponse.RefundOrderId)
}
```

- [ ] **Step 4: 创建 `example/alipay/alipay.fund.trans.toalipay.transfer.go`**

```go
package main

import (
	"log"
	"os"

	"github.com/godrealms/go-aliyun-sdk/alipay"
	"github.com/godrealms/go-aliyun-sdk/alipay/types"
)

func main() {
	// Required env vars: ALIPAY_APP_ID, ALIPAY_PRIVATE_KEY, ALIPAY_PUBLIC_KEY_FROM_ALIPAY
	client := alipay.NewClient()
	client.AppId = os.Getenv("ALIPAY_APP_ID")
	client.PrivateKey = os.Getenv("ALIPAY_PRIVATE_KEY")
	client.AlipayPublicKey = os.Getenv("ALIPAY_PUBLIC_KEY_FROM_ALIPAY")
	client.Sandbox = true
	client.Http.SetBaseURL("https://openapi-sandbox.dl.alipaydev.com/gateway.do")

	resp, err := client.AlipayFundTransToalipayTransfer(&types.FundTransToalipayTransfer{
		OutBizNo:     "biz_20260420_002",
		PayeeType:    "ALIPAY_LOGONID",
		PayeeAccount: "test@example.com",
		Amount:       "5.00",
		Remark:       "奖励发放",
	})
	if err != nil {
		log.Fatal(err)
	}
	log.Printf("Code: %s, OrderId: %s", resp.AlipayFundTransToalipayTransferResponse.Code, resp.AlipayFundTransToalipayTransferResponse.OrderId)
}
```

- [ ] **Step 5: 创建 `example/alipay/alipay.fund.trans.toaccount.transfer.go`**

```go
package main

import (
	"log"
	"os"

	"github.com/godrealms/go-aliyun-sdk/alipay"
	"github.com/godrealms/go-aliyun-sdk/alipay/types"
)

func main() {
	// Required env vars: ALIPAY_APP_ID, ALIPAY_PRIVATE_KEY, ALIPAY_PUBLIC_KEY_FROM_ALIPAY
	client := alipay.NewClient()
	client.AppId = os.Getenv("ALIPAY_APP_ID")
	client.PrivateKey = os.Getenv("ALIPAY_PRIVATE_KEY")
	client.AlipayPublicKey = os.Getenv("ALIPAY_PUBLIC_KEY_FROM_ALIPAY")
	client.Sandbox = true
	client.Http.SetBaseURL("https://openapi-sandbox.dl.alipaydev.com/gateway.do")

	resp, err := client.AlipayFundTransToaccountTransfer(&types.FundTransToaccountTransfer{
		OutBizNo:      "biz_20260420_003",
		PayeeType:     "ALIPAY_LOGONID",
		PayeeAccount:  "test@example.com",
		Amount:        "5.00",
		PayerShowName: "XX 公司",
		Remark:        "工资发放",
	})
	if err != nil {
		log.Fatal(err)
	}
	log.Printf("Code: %s, OrderId: %s", resp.AlipayFundTransToaccountTransferResponse.Code, resp.AlipayFundTransToaccountTransferResponse.OrderId)
}
```

- [ ] **Step 6: 创建 `example/alipay/alipay.fund.trans.common.query.go`**

```go
package main

import (
	"log"
	"os"

	"github.com/godrealms/go-aliyun-sdk/alipay"
	"github.com/godrealms/go-aliyun-sdk/alipay/types"
)

func main() {
	// Required env vars: ALIPAY_APP_ID, ALIPAY_PRIVATE_KEY, ALIPAY_PUBLIC_KEY_FROM_ALIPAY
	client := alipay.NewClient()
	client.AppId = os.Getenv("ALIPAY_APP_ID")
	client.PrivateKey = os.Getenv("ALIPAY_PRIVATE_KEY")
	client.AlipayPublicKey = os.Getenv("ALIPAY_PUBLIC_KEY_FROM_ALIPAY")
	client.Sandbox = true
	client.Http.SetBaseURL("https://openapi-sandbox.dl.alipaydev.com/gateway.do")

	resp, err := client.AlipayFundTransCommonQuery(&types.FundTransCommonQuery{
		OutBizNo: "biz_20260420_002",
	})
	if err != nil {
		log.Fatal(err)
	}
	log.Printf("Status: %s, OrderFee: %s", resp.AlipayFundTransCommonQueryResponse.Status, resp.AlipayFundTransCommonQueryResponse.OrderFee)
}
```

- [ ] **Step 7: 创建 `example/alipay/alipay.fund.joint.frozen.go`**

```go
package main

import (
	"log"
	"os"

	"github.com/godrealms/go-aliyun-sdk/alipay"
	"github.com/godrealms/go-aliyun-sdk/alipay/types"
)

func main() {
	// Required env vars: ALIPAY_APP_ID, ALIPAY_PRIVATE_KEY, ALIPAY_PUBLIC_KEY_FROM_ALIPAY
	client := alipay.NewClient()
	client.AppId = os.Getenv("ALIPAY_APP_ID")
	client.PrivateKey = os.Getenv("ALIPAY_PRIVATE_KEY")
	client.AlipayPublicKey = os.Getenv("ALIPAY_PUBLIC_KEY_FROM_ALIPAY")
	client.Sandbox = true
	client.Http.SetBaseURL("https://openapi-sandbox.dl.alipaydev.com/gateway.do")

	resp, err := client.AlipayFundJointFrozen(&types.FundJointFrozen{
		OutRequestNo: "freeze_20260420_001",
		PayerUserId:  "2088000000000000",
		Amount:       "200.00",
		Remark:       "押金冻结",
	})
	if err != nil {
		log.Fatal(err)
	}
	log.Printf("FreezeId: %s, Amount: %s", resp.AlipayFundJointFrozenResponse.FreezeId, resp.AlipayFundJointFrozenResponse.Amount)
}
```

- [ ] **Step 8: 创建 `example/alipay/alipay.fund.joint.thaw.go`**

```go
package main

import (
	"log"
	"os"

	"github.com/godrealms/go-aliyun-sdk/alipay"
	"github.com/godrealms/go-aliyun-sdk/alipay/types"
)

func main() {
	// Required env vars: ALIPAY_APP_ID, ALIPAY_PRIVATE_KEY, ALIPAY_PUBLIC_KEY_FROM_ALIPAY
	client := alipay.NewClient()
	client.AppId = os.Getenv("ALIPAY_APP_ID")
	client.PrivateKey = os.Getenv("ALIPAY_PRIVATE_KEY")
	client.AlipayPublicKey = os.Getenv("ALIPAY_PUBLIC_KEY_FROM_ALIPAY")
	client.Sandbox = true
	client.Http.SetBaseURL("https://openapi-sandbox.dl.alipaydev.com/gateway.do")

	resp, err := client.AlipayFundJointThaw(&types.FundJointThaw{
		OutRequestNo: "thaw_20260420_001",
		FreezeId:     "冻结单号填此处",
		Amount:       "200.00",
		Remark:       "退还押金",
	})
	if err != nil {
		log.Fatal(err)
	}
	log.Printf("Code: %s, Amount: %s", resp.AlipayFundJointThawResponse.Code, resp.AlipayFundJointThawResponse.Amount)
}
```

- [ ] **Step 9: 创建 `example/alipay/alipay.fund.joint.deduct.go`**

```go
package main

import (
	"log"
	"os"

	"github.com/godrealms/go-aliyun-sdk/alipay"
	"github.com/godrealms/go-aliyun-sdk/alipay/types"
)

func main() {
	// Required env vars: ALIPAY_APP_ID, ALIPAY_PRIVATE_KEY, ALIPAY_PUBLIC_KEY_FROM_ALIPAY
	client := alipay.NewClient()
	client.AppId = os.Getenv("ALIPAY_APP_ID")
	client.PrivateKey = os.Getenv("ALIPAY_PRIVATE_KEY")
	client.AlipayPublicKey = os.Getenv("ALIPAY_PUBLIC_KEY_FROM_ALIPAY")
	client.Sandbox = true
	client.Http.SetBaseURL("https://openapi-sandbox.dl.alipaydev.com/gateway.do")

	resp, err := client.AlipayFundJointDeduct(&types.FundJointDeduct{
		OutRequestNo: "deduct_20260420_001",
		FreezeId:     "冻结单号填此处",
		Amount:       "50.00",
		Remark:       "扣除违约押金",
	})
	if err != nil {
		log.Fatal(err)
	}
	log.Printf("Code: %s, OrderId: %s", resp.AlipayFundJointDeductResponse.Code, resp.AlipayFundJointDeductResponse.OrderId)
}
```

- [ ] **Step 10: Commit**

```bash
git add example/alipay/alipay.fund.trans.uni.transfer.go \
        example/alipay/alipay.fund.trans.order.query.go \
        example/alipay/alipay.fund.trans.refund.go \
        example/alipay/alipay.fund.trans.toalipay.transfer.go \
        example/alipay/alipay.fund.trans.toaccount.transfer.go \
        example/alipay/alipay.fund.trans.common.query.go \
        example/alipay/alipay.fund.joint.frozen.go \
        example/alipay/alipay.fund.joint.thaw.go \
        example/alipay/alipay.fund.joint.deduct.go
git commit -m "feat(fund): 新增资金线示例文件"
```

---

## Task 12: 最终验证与推送

- [ ] **Step 1: 运行所有测试**

```bash
go test ./alipay/ -v
```

预期：全部 `PASS`（含原有 7 个 + 新增 9 个，共 16 个）

- [ ] **Step 2: 构建和静态检查**

```bash
go build github.com/godrealms/go-aliyun-sdk/alipay \
         github.com/godrealms/go-aliyun-sdk/alipay/types \
         github.com/godrealms/go-aliyun-sdk/community \
         github.com/godrealms/go-aliyun-sdk/utils
go vet ./alipay/ ./alipay/types/
```

预期：无错误输出

- [ ] **Step 3: 推送到远端**

```bash
git push origin develop
```
